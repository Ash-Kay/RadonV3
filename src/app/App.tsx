import React from "react";
import Navbar from "./components/Navbar";

interface Props {}

const App = (props: Props) => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default App;
